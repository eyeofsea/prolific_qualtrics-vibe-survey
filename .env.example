# 강사용 — 이 파일을 .env 로 복사하거나 셸 환경에 export 하세요.
# .env 는 .gitignore 에 포함되어 있어 커밋되지 않습니다.
#
# Streamlit 의 .streamlit/secrets.toml 을 우선 사용하지만,
# CLI/CI 환경에서는 이 환경변수가 fallback 으로 쓰입니다.

QUALTRICS_API_TOKEN=YOUR_QUALTRICS_API_TOKEN
QUALTRICS_DATA_CENTER=syd1
